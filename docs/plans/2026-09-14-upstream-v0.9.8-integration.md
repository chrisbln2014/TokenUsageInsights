# 整合 upstream v0.9.8 計畫(2026-09-14)

## 目標
把 `upstream/main`(doggy8088/TokenUsageInsights v0.9.8,自 v0.2.2 起 291 commits)併入本 fork,保留 Cloud Run + Google Drive snapshot 自訂功能。

## 限制
- 不推 upstream(push 已 DISABLED);不動使用者正在跑的本機看板(PID 佔 3003)與正式 DB(`%LOCALAPPDATA%\TokenUsageInsights\token_usage_insights.db`)。
- 所有驗證用 `INSIGHTS_DIR` 指向暫存目錄 + 正式 DB 的複本;新版會跑 migration(codex v7 重解析、`system_metadata`),不得碰正式 DB。
- 合併前建立備份標籤 `backup/improve-pre-v0.9.8`、`backup/feature-pre-v0.9.8`。

## 步驟
1. `improve` fast-forward 到 `origin/improve`,併入 `upstream/main`,解 `static/app.js`、`static/index.html` 衝突。
2. `feature/cloud-run-drive-snapshot` 併入更新後的 `improve`,解 `src/main.rs`(把 snapshot 分支嫁接到 upstream 新 `main()`)與 `static/app.js`。
3. 語意修正:
   - `src/main.rs`:`--export-snapshot` 必須在 `cli::run` 之前攔截(否則 exit 2「未知指令」);snapshot 模式跳過 updater 救援/自動更新/DB 初始化/服務交接。
   - `src/snapshot.rs`:`ASSISTANTS` 補 grok/pi/omp/muse,並以測試釘住與 `handlers::is_supported_assistant` 一致;`get_setup_info` 同步。
   - snapshot router 補 upstream 新增 GET 端點(`model-sessions`、`session-search`)的唯讀回應,避免前端打到非 JSON 404。
   - `Dockerfile`:`TOKEN_USAGE_INSIGHTS_AUTO_UPDATE=0`。
   - `scripts/upload-drive-snapshot.ps1`:`$Assistants` 補新 agent。
   - 文件標註:fork 版安裝到 `%LOCALAPPDATA%\TokenUsageInsights` 後,自動更新會換成 doggy8088 官方版(無 snapshot 功能),需關閉自動更新。

## 驗收條件
- [x] `cargo build --release` 無錯;`cargo test` 全綠(含 upstream `tests/cli.rs`、snapshot 測試、新增 assistant 名單測試)。
- [x] `node --test tests/*.test.*` 全綠(upstream 前端/npm 測試)。
- [x] 突變:從 `ASSISTANTS` 移除 `muse` → 名單測試紅在該斷言。
- [x] Live:`token-usage-insights --export-snapshot <tmp>`(INSIGHTS_DIR=暫存複本)成功產出含新 agent 鍵的 JSON。
- [x] Live:`TOKEN_USAGE_INSIGHTS_SNAPSHOT_PATH=<tmp>` + `PORT=18714(3004–3203 為 Windows 保留埠)` 啟動,`/api/claude/dates`、`/api/muse/dates`、`/api/claude/usage/<date>`、`/api/claude/model-sessions` 回 JSON;不建立 DB、不觸發更新檢查。
- [x] Live:一般模式 `PORT=18715` + INSIGHTS_DIR 暫存複本啟動,`/api/claude/dates` 正常。
- [x] reviewer subagent 獨立審查 main.rs 嫁接與 snapshot 修正。

## 已排除的路
- 直接 `git merge upstream/main` 到 feature 分支一次解完:衝突與無關改動糾纏,改兩段合併。
- 把 `--export-snapshot` 加進 upstream `cli.rs` 的 match:擴大與 upstream 未來衝突面,改在 `main()` 呼叫 `cli::run` 前攔截。

## 執行結果（2026-09-14）
- 已完成：兩段合併、main.rs 嫁接、snapshot.rs 改為重用 upstream handler、Dockerfile／ps1／文件更新；opus reviewer 審查（fable 429 fallback）後依 findings 修正並補測試，詳見 PROGRESS.md 同日條目。
- 已排除的路（補）：修補 snapshot.rs 複製版彙總邏輯以通過編譯——會與 upstream 成本計算持續分歧，改為同進程呼叫 handler；匯出時呼叫 `migrate_old_databases`——會改名家目錄下的舊 DB，移除。
